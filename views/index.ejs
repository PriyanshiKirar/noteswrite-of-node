<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-200">
        <div class="form py-10 px-10">
            <form action="/create" method="post">
                <input class="block w-full px-5 py-2 rounded-md outline-none" type="text" placeholder="name" name="name">
                <textarea class=" block px-5 py-2 w-full mt-2 outline-none resize-none rounded-md"
                    placeholder="Write something about task..." name="details"></textarea>
                <input class="text-md  mt-5 px-10 py-3 bg-blue-600 rounded-md" type="submit">
            </form>
        </div>
        <div class="tasks flex gap-10 flex-wrap px-10 py-10">
            <% if(files.length>0){ %>

                <%files.forEach(function(file){ %>
                    <div class="task  w-72 p-3 bg-white rounded-lg">
                        <h3 class="text-2xl tracking-tighter mb-3">
                            <%=file.name%>
                        </h3>
                        <p>
                            <%=file.content%>
                        </p>
                        <div class="flex justify-between">
                            <a href="/read/<%=file.name%>"
                                class="text-1xl text-blue-500 uppercase mt-5 inline-block">read
                                more</a>
                            <a class="text-green-800 text-xl  mt-5 inline-block" href="/edit/<%=file.name%>">Edit</a>
                            <a class="text-red-500 ml-5 text-xl mt-5 inline-block"
                                href="/delete/<%=file.name%>">delete</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else {%>

                            <h3 class=" text-xl text-zinc-400">no tasks yet</h3>
                            <% }%>
        </div>
    </div>
</body>

</html>